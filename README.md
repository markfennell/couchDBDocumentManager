couchDBDocumentManager
======================

Upload, search, update documents with attachments in CouchDB. 
Requires CouchDB-Lucene.
Requires database named docs with fields for docTitle and docName.

